source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins

# `gem install bundler` to get bundle
# `bundle install` treats Gemfile.lock as authoritative
# `bundle update` uses this to update dependencies
